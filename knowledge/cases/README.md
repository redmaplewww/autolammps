# Case Library

This directory stores the unified LAMMPS case corpus.

- `case-family-index.md` maps tasks into normalized case families
- `CASE_METADATA.md` defines per-case metadata, including retrieval weight
- `raw/` contains the original case folders and their `case.metadata.json` files
