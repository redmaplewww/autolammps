# CC CLI 知识库检索与问答准确度测试报告

- 日期: 2026-04-25T00:09:10.121Z
- 测试对象: 当前仓库中的 CC CLI（LAMMPS 知识库检索路径）
- 执行方式: 对 50 道中文检索问答题逐题执行 `searchKnowledge()` 本地索引检索，评估知识库检索命中与可回答性
- 评分标准: 每题 0-4 分（命令成功 1 分 + 引用命中 1 分 + 主断言命中 1 分 + 次断言命中 1 分）

## 总评

- 总分: 171 / 200
- 平均分: 3.42 / 4
- 百分制: 85.5%

## 分项成绩

| 模块 | 得分 | 百分比 | 题目数 |
| --- | ---: | ---: | ---: |
| 输入规则 | 35/40 | 87.5% | 10 |
| 势函数 | 32/40 | 80.0% | 10 |
| 命令语法 | 34/40 | 85.0% | 10 |
| 案例检索 | 36/40 | 90.0% | 10 |
| 工作流认知 | 34/40 | 85.0% | 10 |

## 低分题目

- Q06 2/4: 为什么一个模拟 run 完且没报错，仍然不能直接算成功？请按知识库规则回答。
- Q11 2/4: ReaxFF 输入最关键的电荷处理检查是什么？请按本地 confirmed lesson 回答。
- Q18 2/4: 审查 force field 映射时，知识库的 checklist 要核对哪几项？
- Q23 2/4: 压缩时 erate 的正负号怎么判定？请给出知识库中的说法。

## 逐题明细

| 题号 | 模块 | 得分 | 用时 | 题目 | 说明 |
| --- | --- | ---: | ---: | --- | --- |
| Q01 | 输入规则 | 3/4 | 161 ms | 为什么工程应变不能直接用 variable L0 equal lx 动态计算？请给出本地知识库依据。 | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q02 | 输入规则 | 4/4 | 41 ms | fix deform 控制 x 拉伸时，fix npt 能不能同时控制 x 压力？为什么？请引用本地规则。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q03 | 输入规则 | 4/4 | 169 ms | NPT 平衡后切换到 fix deform 之前，知识库建议检查哪些 thermo 量来确认平衡稳定？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q04 | 输入规则 | 3/4 | 156 ms | 模型里有固定原子组时，为什么不能直接 velocity all create？风险是什么？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q05 | 输入规则 | 4/4 | 28 ms | delete_atoms overlap 为什么不能在敏感结构上用 all all 宽泛删除？本地经验怎么说？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q06 | 输入规则 | 2/4 | 13 ms | 为什么一个模拟 run 完且没报错，仍然不能直接算成功？请按知识库规则回答。 | 命令成功 / 命中引用 / 未命中主断言 / 未命中次断言 |
| Q07 | 输入规则 | 4/4 | 18 ms | 从 restart 或 data 文件继续运行前，为什么必须核对来源、生成时间和盒子尺寸？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q08 | 输入规则 | 4/4 | 181 ms | 高风险 LAMMPS 输入文件在开写前为什么要先搜索本地案例而不是凭记忆写？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q09 | 输入规则 | 4/4 | 37 ms | NiC 球形沉积迁移时 deposit_re_lo 和 deposit_re_up 能直接复制吗？为什么？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q10 | 输入规则 | 3/4 | 25 ms | 迁移沉积案例时，除了重算 deposit_re_lo 和 deposit_re_up，还要额外检查哪一个 mobile 区域假设？ | 命令成功 / 命中引用 / 未命中主断言 / 命中次断言 |
| Q11 | 势函数 | 2/4 | 23 ms | ReaxFF 输入最关键的电荷处理检查是什么？请按本地 confirmed lesson 回答。 | 命令成功 / 命中引用 / 未命中主断言 / 未命中次断言 |
| Q12 | 势函数 | 3/4 | 177 ms | 为什么 ReaxFF 的 pair_coeff 元素顺序必须和 data 文件 atom type 顺序一致？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q13 | 势函数 | 4/4 | 169 ms | Cu FCC 单晶压缩在本地知识库里优先推荐什么势函数和 pair_style？请给出本地案例依据。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q14 | 势函数 | 3/4 | 33 ms | 什么时候本地知识库会倾向用 MEAM，而不是简单 EAM？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q15 | 势函数 | 4/4 | 181 ms | 反应沉积、氧化、腐蚀这类问题在本地知识库里通常对应哪类势函数家族？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q16 | 势函数 | 3/4 | 167 ms | SiC 或类似共价体系在本地知识库里通常先查哪类势函数？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q17 | 势函数 | 3/4 | 166 ms | 做势函数推荐时，MVP 规则要求至少给出什么本地证据？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q18 | 势函数 | 2/4 | 25 ms | 审查 force field 映射时，知识库的 checklist 要核对哪几项？ | 命令成功 / 命中引用 / 未命中主断言 / 未命中次断言 |
| Q19 | 势函数 | 4/4 | 24 ms | Ni 基合金拉伸问题从案例库检索时，应该先看哪个 case family？给出路径依据。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q20 | 势函数 | 4/4 | 51 ms | 高熵合金熔化凝固问题从案例库检索时，应该先看哪个 case family？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q21 | 命令语法 | 4/4 | 15 ms | fix nvt 的标准语法是什么？请引用本地手册路径。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q22 | 命令语法 | 3/4 | 38 ms | fix deform 的标准语法是什么？请引用本地手册路径。 | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q23 | 命令语法 | 2/4 | 13 ms | 压缩时 erate 的正负号怎么判定？请给出知识库中的说法。 | 命令成功 / 命中引用 / 未命中主断言 / 未命中次断言 |
| Q24 | 命令语法 | 4/4 | 28 ms | 固体压缩或拉伸时为什么常用 remap x？它和 remap v 的区别是什么？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q25 | 命令语法 | 4/4 | 20 ms | Tdamp 在 metal 单位下通常怎么按 timestep 选取？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q26 | 命令语法 | 4/4 | 14 ms | read_restart 和 read_data 的继续运行前检查重点有哪些？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q27 | 命令语法 | 3/4 | 174 ms | pair_coeff 在知识库里的审查重点是什么？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q28 | 命令语法 | 3/4 | 29 ms | metal 单位下把 pxx 转成 GPa 的常见换算写法是什么？请给本地案例依据。 | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q29 | 命令语法 | 4/4 | 21 ms | Cu 单轴压缩案例里，应变和应力变量是怎么定义的？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q30 | 命令语法 | 3/4 | 32 ms | fix print 输出应变时，为什么容易把冻结长度规则写错？知识库里怎么提醒？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q31 | 案例检索 | 4/4 | 16 ms | Cu FCC 压缩问题从案例库检索时，最佳匹配族是什么？请给出路径依据。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q32 | 案例检索 | 4/4 | 28 ms | 拉伸问题从案例库检索时应该先看哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q33 | 案例检索 | 3/4 | 32 ms | 剪切模量或 TiAlNb 剪切问题应该先检索哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q34 | 案例检索 | 4/4 | 65 ms | NiC、GaN 氧化、金沉积、腐蚀这类问题应该先检索哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q35 | 案例检索 | 3/4 | 27 ms | 磨削、摩擦、界面类问题在案例库里应该先检索哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q36 | 案例检索 | 3/4 | 47 ms | shock、PKA 级联、TTM 这类问题应该先检索哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q37 | 案例检索 | 3/4 | 86 ms | NiPt、NiTi、B2 相、偏聚相关问题应该先检索哪个 family？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q38 | 案例检索 | 4/4 | 5 ms | 知识库建议的案例检索顺序是什么？请按步骤回答。 | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q39 | 案例检索 | 4/4 | 18 ms | raw cases 应该被当成最终正确模板吗？知识库怎么界定它们？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q40 | 案例检索 | 4/4 | 59 ms | 检索时更应该先查 raw cases 还是只看 index 概述？知识库推荐流程是什么？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q41 | 工作流认知 | 3/4 | 25 ms | 金属双晶或晶界拉伸时，为什么横向边界条件必须被当成首要变量？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q42 | 工作流认知 | 4/4 | 7 ms | 为什么纳米晶金属研究必须把晶粒尺寸当成一等扫描变量？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q43 | 工作流认知 | 3/4 | 184 ms | 增材制造金属拉伸为什么不能只用一个泛化多晶模型替代？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q44 | 工作流认知 | 3/4 | 15 ms | 金属晶界研究为什么不能只记录 Sigma 值？还应记录哪些边界描述符？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q45 | 工作流认知 | 3/4 | 182 ms | Ni 基高温合金分析为什么不能只看基体？知识库强调哪几层因素？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q46 | 工作流认知 | 3/4 | 35 ms | 为什么不能用一个代表性的 HEA 成分去泛化整个 HEA 类别？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q47 | 工作流认知 | 3/4 | 23 ms | 纯金属机理迁移为什么要先区分 FCC、BCC、HCP，而不是都按 generic metal 处理？ | 命令成功 / 命中引用 / 命中主断言 / 未命中次断言 |
| Q48 | 工作流认知 | 4/4 | 81 ms | 辐照、扩散、偏聚问题的证据组织链条应该是什么？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q49 | 工作流认知 | 4/4 | 171 ms | 金属切削分析为什么不能只看切削力和切屑？知识库要求补哪些输出？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
| Q50 | 工作流认知 | 4/4 | 163 ms | 表面加工或纳米压痕分析为什么不能只看形貌？还必须配合哪些缺陷分析？ | 命令成功 / 命中引用 / 命中主断言 / 命中次断言 |
